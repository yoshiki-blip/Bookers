<h1>編集画面</h1>
<!-- form_for部分 -->
<%= form_for(@book) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
    <%= link_to "Show", book_path(@book) %>
	<%= link_to "Back", books_path %>
<% end %>
